<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lrs.test">
    

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".application.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/BaseAppTheme">
        <activity
            android:name=".activity.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.facebook.react.devsupport.DevSettingsActivity" />
        <activity
            android:name=".activity.rn.ReactNativeActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />
        <activity android:name=".activity.rx.RxActivity" />
        <activity android:name=".activity.widget.ImagePickerActivity" />
        <activity android:name=".mvp.LoginActivity" />
        <activity android:name=".activity.common.GlideTestActivity" />
        <activity android:name=".activity.widget.DatePickerActivity" />
        <activity android:name=".activity.widget.DatePickActivity2" />
        <activity android:name=".activity.widget.DatePickerDialogActivity" />
        <activity android:name=".activity.widget.MaterialCalendarViewActivity" />
        <activity android:name=".activity.widget.SurfaceViewActivity" />
        <activity android:name=".activity.widget.UiStudyActivity" />
        <activity android:name=".activity.common.wechat.WeChatMainActivity"/>
    </application>

</manifest>